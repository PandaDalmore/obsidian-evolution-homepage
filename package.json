{
  "name": "evolution-homepage",
  "version": "1.0.5",
  "description": "A configurable personal homepage for Obsidian.",
  "license": "MIT",
  "author": "PandaDal",
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "homepage",
    "dashboard",
    "tasks",
    "calendar"
  ],
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "check:version": "node scripts/check-version.mjs",
    "bump": "node scripts/bump-version.mjs",
    "build": "npm run check:version && tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "obsidian": "latest",
    "typescript": "^5.8.0"
  }
}
